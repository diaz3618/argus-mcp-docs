---
phase: 05-catalog-expansion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/exa-remote.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/linear-remote.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/remote-sse/exa-sse.yaml
autonomous: true
requirements:
  - CAT-EXP-01
  - CAT-EXP-07

must_haves:
  truths:
    - "remote-http and remote-sse categories have new entries with proper Bearer token auth patterns"
    - "All remote entries use ${SECRET_NAME} syntax for API keys and tokens"
    - "All new remote entries are structurally valid YAML with name, description, and backend slug"
  artifacts:
    - path: "/home/diaz/mygit/argus-mcp-catalog/configs/remote-http/linear-remote.yaml"
      provides: "Bearer token auth pattern for remote HTTP MCP"
      contains: "Authorization"
    - path: "/home/diaz/mygit/argus-mcp-catalog/configs/remote-sse/exa-sse.yaml"
      provides: "SSE remote with Bearer token"
      contains: "type: sse"
  key_links:
    - from: "linear-remote.yaml"
      to: "headers.Authorization"
      via: "Bearer ${LINEAR_API_KEY} for Linear MCP hosted endpoint"
      pattern: "Bearer"
---

<objective>
Add 3 new remote server entries across remote-http and remote-sse categories, covering popular hosted MCP endpoints with proper Bearer token authentication patterns.

Purpose: Fills two thin categories (remote-http had 2 entries, remote-sse had 2 entries) with real, popular hosted MCP servers. Demonstrates Bearer token auth pattern for remote HTTP/SSE servers. Per D-01, CAT-EXP-01, CAT-EXP-07.
Output: 3 YAML files in argus-mcp-catalog/configs/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-catalog-expansion/05-CONTEXT.md
@.planning/phases/05-catalog-expansion/05-RESEARCH.md

<interfaces>
<!-- Existing remote server patterns for reference -->

From configs/remote-http/semgrep.yaml (Bearer token pattern):
```yaml
name: "Semgrep"
description: "Remote MCP server for Semgrep static analysis via HTTP."

semgrep:
  type: http
  url: "https://mcp.semgrep.dev/mcp"
  headers:
    Authorization: "Bearer ${SEMGREP_TOKEN}"
```

From configs/remote-sse/deepwiki.yaml (no-auth SSE pattern):
```yaml
name: "DeepWiki"
description: "Remote MCP server providing deep wiki knowledge lookup via SSE."

deepwiki:
  type: sse
  url: "https://mcp.deepwiki.com/sse"
```

From CONTRIBUTING.md field reference:
- type: http or streamable-http for HTTP endpoints
- type: sse for SSE endpoints
- url: Full URL for remote servers
- headers: HTTP headers; use ${ENV_VAR} for secrets
- Remote servers never have a -container.yaml variant
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create remote-http and remote-sse entries (Exa, Linear)</name>
  <files>
    /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/exa-remote.yaml
    /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/linear-remote.yaml
    /home/diaz/mygit/argus-mcp-catalog/configs/remote-sse/exa-sse.yaml
  </files>
  <read_first>
    - /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/semgrep.yaml (Bearer token pattern reference)
    - /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/plantuml.yaml (no-auth HTTP reference)
    - /home/diaz/mygit/argus-mcp-catalog/configs/remote-sse/deepwiki.yaml (SSE pattern reference)
    - /home/diaz/mygit/argus-mcp-catalog/CONTRIBUTING.md (remote server naming rules)
  </read_first>
  <action>
Create exactly the following 3 YAML files:

**configs/remote-http/exa-remote.yaml**
```yaml
name: "Exa Search (remote HTTP)"
description: "Semantic web search powered by Exa AI. Connects to the hosted Exa MCP endpoint via HTTP with Bearer token authentication."
# Secrets: EXA_API_KEY — set with: argus-mcp secrets set EXA_API_KEY <your-exa-api-key>
# Get key at: https://dashboard.exa.ai/api-keys
# Remote alternative to the local exa-container entry — no Docker required

exa-remote:
  type: streamable-http
  url: "https://mcp.exa.ai/mcp"
  headers:
    Authorization: "Bearer ${EXA_API_KEY}"
```

**configs/remote-http/linear-remote.yaml**
```yaml
name: "Linear (remote HTTP)"
description: "Manage Linear issues, projects, and teams via the official Linear hosted MCP server. Connects via HTTP with Bearer token authentication."
# Secrets: LINEAR_API_KEY — set with: argus-mcp secrets set LINEAR_API_KEY <your-linear-api-key>
# Get key at: https://linear.app/settings/api
# Remote alternative to the local linear subprocess entry — no local package install required

linear-remote:
  type: streamable-http
  url: "https://mcp.linear.app/sse"
  headers:
    Authorization: "Bearer ${LINEAR_API_KEY}"
```

**configs/remote-sse/exa-sse.yaml**
```yaml
name: "Exa Search (SSE)"
description: "Semantic web search powered by Exa AI. Connects to the hosted Exa MCP endpoint via Server-Sent Events with Bearer token authentication."
# Secrets: EXA_API_KEY — set with: argus-mcp secrets set EXA_API_KEY <your-exa-api-key>
# Get key at: https://dashboard.exa.ai/api-keys

exa-sse:
  type: sse
  url: "https://mcp.exa.ai/mcp"
  headers:
    Authorization: "Bearer ${EXA_API_KEY}"
```
  </action>
  <verify>
    <automated>cd /home/diaz/mygit/argus-mcp-catalog && for f in configs/remote-http/exa-remote.yaml configs/remote-http/linear-remote.yaml configs/remote-sse/exa-sse.yaml; do node -e "const y=require('js-yaml'); const d=y.load(require('fs').readFileSync('$f','utf8')); if(!d.name||!d.description) throw new Error('missing name/desc in $f'); console.log('OK: $f')"; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "EXA_API_KEY" /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/exa-remote.yaml` returns a match
    - `grep "LINEAR_API_KEY" /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/linear-remote.yaml` returns a match
    - `grep "type: streamable-http" /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/exa-remote.yaml` returns a match
    - `grep "type: sse" /home/diaz/mygit/argus-mcp-catalog/configs/remote-sse/exa-sse.yaml` returns a match
    - `grep "exa-remote:" /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/exa-remote.yaml` returns a match
    - `grep "linear-remote:" /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/linear-remote.yaml` returns a match
    - `grep "exa-sse:" /home/diaz/mygit/argus-mcp-catalog/configs/remote-sse/exa-sse.yaml` returns a match
    - None of the files contain hardcoded API key values — only ${SECRET_NAME} syntax
    - All 3 files exist and are non-empty
  </acceptance_criteria>
  <done>3 remote server YAML files created: exa-remote (HTTP), linear-remote (HTTP), exa-sse (SSE) — all with Bearer ${SECRET_NAME} auth patterns</done>
</task>

</tasks>

<verification>
After task completes:
```bash
ls /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/
ls /home/diaz/mygit/argus-mcp-catalog/configs/remote-sse/
grep -r "Bearer" /home/diaz/mygit/argus-mcp-catalog/configs/remote-http/
grep -r "Bearer" /home/diaz/mygit/argus-mcp-catalog/configs/remote-sse/
```

Note: catalog.json is NOT updated in this plan — handled by plan 05-06 (Wave 2).
</verification>

<success_criteria>
- 3 YAML files created across remote-http and remote-sse categories
- All use ${SECRET_NAME} syntax for API keys (per CAT-EXP-07)
- Backend slugs match filename stems exactly
- No hardcoded credentials
</success_criteria>

<output>
After completion, create `/home/diaz/mygit/argus-mcp-docs/.planning/phases/05-catalog-expansion/05-04-SUMMARY.md`
</output>
