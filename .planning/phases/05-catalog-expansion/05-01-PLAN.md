---
phase: 05-catalog-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/diaz/mygit/argus-mcp-catalog/configs/databases/sqlite-container.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/databases/sqlite.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/databases/mysql-container.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/databases/mysql.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/databases/redis-container.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/databases/redis.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/databases/mongodb-container.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/databases/neon-container.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/web-research/playwright-container.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/web-research/exa-container.yaml
  - /home/diaz/mygit/argus-mcp-catalog/configs/web-research/exa.yaml
autonomous: true
requirements:
  - CAT-EXP-01
  - CAT-EXP-07

must_haves:
  truths:
    - "11 new YAML catalog entries exist in databases/ and web-research/ directories"
    - "sqlite-container.yaml demonstrates container.volumes with network: none"
    - "mysql-container.yaml, redis-container.yaml, mongodb-container.yaml, neon-container.yaml demonstrate bridge network with env secrets using ${SECRET_NAME} syntax"
    - "playwright-container.yaml demonstrates container.system_deps for browser automation"
    - "All YAML files parse without error and have name, description, and backend slug keys"
  artifacts:
    - path: "/home/diaz/mygit/argus-mcp-catalog/configs/databases/sqlite-container.yaml"
      provides: "Volume mount pattern demo (network: none, volumes)"
      contains: "container.volumes"
    - path: "/home/diaz/mygit/argus-mcp-catalog/configs/databases/mysql-container.yaml"
      provides: "npm container with env secrets"
      contains: "MYSQL_CONNECTION_STRING"
    - path: "/home/diaz/mygit/argus-mcp-catalog/configs/web-research/playwright-container.yaml"
      provides: "system_deps pattern demo"
      contains: "system_deps"
  key_links:
    - from: "sqlite-container.yaml"
      to: "container.volumes"
      via: "uvx mcp-server-sqlite with --db-path /data/db.sqlite"
      pattern: "volumes:"
    - from: "playwright-container.yaml"
      to: "container.system_deps"
      via: "npx @playwright/mcp with chromium packages"
      pattern: "system_deps:"
---

<objective>
Add 11 new catalog entries to the databases/ and web-research/ categories covering volume mount patterns, bridge network containers with env secrets, and system_deps for browser dependencies.

Purpose: These entries demonstrate three high-priority unrepresented patterns — container.volumes for database file access (sqlite), env secrets with ${SECRET_NAME} syntax across database servers, and container.system_deps for OS-level dependencies (Playwright). Per D-05, D-06, D-09, CAT-EXP-01, CAT-EXP-07.
Output: 11 YAML files in argus-mcp-catalog/configs/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-catalog-expansion/05-CONTEXT.md
@.planning/phases/05-catalog-expansion/05-RESEARCH.md

<interfaces>
<!-- Existing catalog patterns to replicate exactly. Extracted from codebase. -->

From configs/databases/postgres-container.yaml:
```yaml
name: "PostgreSQL Server (containerized)"
description: "Query PostgreSQL databases. Runs in a container with outbound network access to reach your database endpoint."

postgres:
  type: stdio
  command: npx
  args:
    - "-y"
    - "@modelcontextprotocol/server-postgres"
    - "${POSTGRES_CONNECTION_STRING}"
  container:
    enabled: true
    network: bridge
```

From configs/filesystem-access/git-container.yaml:
```yaml
name: "Git Server (containerized)"
description: "Interact with Git repositories. Runs in an isolated container with read-only workspace mount."

git:
  type: stdio
  command: uvx
  args:
    - "mcp-server-git"
    - "--repository"
    - "/workspace"
  container:
    enabled: true
    network: none
    volumes:
      - "$HOME/projects:/workspace:ro"
```

Lint rules (all must pass):
1. catalog.json valid JSON with categories + updated_at
2. Every filename in catalog.json exists at configs/category/filename
3. Every YAML parses without error
4. Every YAML has top-level name: (non-empty string)
5. Every YAML has top-level description: (non-empty string)
6. Every YAML has at least one key besides name and description (backend slug)

Backend slug rule: slug key must match filename stem exactly.
Examples: sqlite-container.yaml → slug key sqlite-container:
         sqlite.yaml → slug key sqlite:
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create database container entries (sqlite, mysql, redis, mongodb, neon)</name>
  <files>
    /home/diaz/mygit/argus-mcp-catalog/configs/databases/sqlite-container.yaml
    /home/diaz/mygit/argus-mcp-catalog/configs/databases/sqlite.yaml
    /home/diaz/mygit/argus-mcp-catalog/configs/databases/mysql-container.yaml
    /home/diaz/mygit/argus-mcp-catalog/configs/databases/mysql.yaml
    /home/diaz/mygit/argus-mcp-catalog/configs/databases/redis-container.yaml
    /home/diaz/mygit/argus-mcp-catalog/configs/databases/redis.yaml
    /home/diaz/mygit/argus-mcp-catalog/configs/databases/mongodb-container.yaml
    /home/diaz/mygit/argus-mcp-catalog/configs/databases/neon-container.yaml
  </files>
  <read_first>
    - /home/diaz/mygit/argus-mcp-catalog/configs/databases/postgres-container.yaml (pattern reference)
    - /home/diaz/mygit/argus-mcp-catalog/configs/databases/postgres.yaml (subprocess pattern reference)
    - /home/diaz/mygit/argus-mcp-catalog/configs/filesystem-access/git-container.yaml (volumes pattern reference)
    - /home/diaz/mygit/argus-mcp-catalog/CONTRIBUTING.md (naming rules)
  </read_first>
  <action>
Create exactly the following 8 YAML files. Use the exact content shown — do not invent fields.

**configs/databases/sqlite-container.yaml**
```yaml
name: "SQLite Server (containerized)"
description: "Query and manage SQLite databases. Runs in an isolated container with a host volume mount for database file access."
# Secrets: none required
# Demonstrates: container.volumes (data persistence), network: none (fully isolated)

sqlite-container:
  type: stdio
  command: uvx
  args:
    - "mcp-server-sqlite"
    - "--db-path"
    - "/data/db.sqlite"
  container:
    enabled: true
    network: none
    volumes:
      - "${HOME}/.mcp-data/sqlite:/data:rw"
```

**configs/databases/sqlite.yaml**
```yaml
name: "SQLite Server"
description: "Query and manage SQLite databases. Runs as a subprocess with direct host filesystem access."

sqlite:
  type: stdio
  command: uvx
  args:
    - "mcp-server-sqlite"
    - "--db-path"
    - "/path/to/your/database.sqlite"
```

**configs/databases/mysql-container.yaml**
```yaml
name: "MySQL Server (containerized)"
description: "Connect to MySQL databases. Runs in a container with outbound network access to reach your MySQL endpoint."
# Secrets: MYSQL_CONNECTION_STRING — set with: argus-mcp secrets set MYSQL_CONNECTION_STRING mysql://user:pass@host:3306/db

mysql-container:
  type: stdio
  command: npx
  args:
    - "-y"
    - "@f4ww4z/mcp-mysql-server"
  env:
    MYSQL_CONNECTION_STRING: "${MYSQL_CONNECTION_STRING}"
  container:
    enabled: true
    network: bridge
```

**configs/databases/mysql.yaml**
```yaml
name: "MySQL Server"
description: "Connect to MySQL databases. Runs as a subprocess."
# Secrets: MYSQL_CONNECTION_STRING — set with: argus-mcp secrets set MYSQL_CONNECTION_STRING mysql://user:pass@host:3306/db

mysql:
  type: stdio
  command: npx
  args:
    - "-y"
    - "@f4ww4z/mcp-mysql-server"
  env:
    MYSQL_CONNECTION_STRING: "${MYSQL_CONNECTION_STRING}"
```

**configs/databases/redis-container.yaml**
```yaml
name: "Redis Server (containerized)"
description: "Interact with Redis key-value stores. Runs in a container with outbound network access to reach your Redis endpoint."
# Secrets: REDIS_URL — set with: argus-mcp secrets set REDIS_URL redis://localhost:6379

redis-container:
  type: stdio
  command: npx
  args:
    - "-y"
    - "@modelcontextprotocol/server-redis"
    - "${REDIS_URL}"
  container:
    enabled: true
    network: bridge
```

**configs/databases/redis.yaml**
```yaml
name: "Redis Server"
description: "Interact with Redis key-value stores. Runs as a subprocess."
# Secrets: REDIS_URL — set with: argus-mcp secrets set REDIS_URL redis://localhost:6379

redis:
  type: stdio
  command: npx
  args:
    - "-y"
    - "@modelcontextprotocol/server-redis"
    - "${REDIS_URL}"
```

**configs/databases/mongodb-container.yaml**
```yaml
name: "MongoDB Server (containerized)"
description: "Connect to MongoDB databases. Runs in a container with outbound network access to reach your MongoDB endpoint."
# Secrets: MONGODB_CONNECTION_STRING — set with: argus-mcp secrets set MONGODB_CONNECTION_STRING mongodb://user:pass@host:27017/db

mongodb-container:
  type: stdio
  command: npx
  args:
    - "-y"
    - "mongodb-mcp-server"
  env:
    MDB_MCP_CONNECTION_STRING: "${MONGODB_CONNECTION_STRING}"
  container:
    enabled: true
    network: bridge
```

**configs/databases/neon-container.yaml**
```yaml
name: "Neon Serverless Postgres (containerized)"
description: "Manage Neon serverless PostgreSQL projects and databases via the Neon API. Runs in a container with outbound network access."
# Secrets: NEON_API_KEY — set with: argus-mcp secrets set NEON_API_KEY <your-neon-api-key>
# Get key at: https://console.neon.tech/app/settings/api-keys

neon-container:
  type: stdio
  command: npx
  args:
    - "-y"
    - "@neondatabase/mcp-server-neon"
    - "start"
    - "${NEON_API_KEY}"
  container:
    enabled: true
    network: bridge
```
  </action>
  <verify>
    <automated>cd /home/diaz/mygit/argus-mcp-catalog && node scripts/lint-catalog.js 2>&1 | grep -E "sqlite|mysql|redis|mongodb|neon|error|Error" || true</automated>
  </verify>
  <acceptance_criteria>
    - `ls /home/diaz/mygit/argus-mcp-catalog/configs/databases/` shows: mongodb-container.yaml, mysql-container.yaml, mysql.yaml, neon-container.yaml, redis-container.yaml, redis.yaml, sqlite-container.yaml, sqlite.yaml (plus existing postgres and supabase files)
    - `grep "volumes:" /home/diaz/mygit/argus-mcp-catalog/configs/databases/sqlite-container.yaml` returns a match
    - `grep "network: none" /home/diaz/mygit/argus-mcp-catalog/configs/databases/sqlite-container.yaml` returns a match
    - `grep "MYSQL_CONNECTION_STRING" /home/diaz/mygit/argus-mcp-catalog/configs/databases/mysql-container.yaml` returns a match
    - `grep "NEON_API_KEY" /home/diaz/mygit/argus-mcp-catalog/configs/databases/neon-container.yaml` returns a match
    - `grep "sqlite-container:" /home/diaz/mygit/argus-mcp-catalog/configs/databases/sqlite-container.yaml` returns a match (slug matches filename stem)
    - All 8 files exist and are non-empty
  </acceptance_criteria>
  <done>8 database YAML files created with correct patterns: sqlite volume mount, mysql/redis/mongodb/neon bridge network with ${SECRET_NAME} secrets</done>
</task>

<task type="auto">
  <name>Task 2: Create web-research container entries (playwright, exa)</name>
  <files>
    /home/diaz/mygit/argus-mcp-catalog/configs/web-research/playwright-container.yaml
    /home/diaz/mygit/argus-mcp-catalog/configs/web-research/exa-container.yaml
    /home/diaz/mygit/argus-mcp-catalog/configs/web-research/exa.yaml
  </files>
  <read_first>
    - /home/diaz/mygit/argus-mcp-catalog/configs/web-research/brave-search-container.yaml (pattern reference)
    - /home/diaz/mygit/argus-mcp-catalog/configs/web-research/fetch-container.yaml (pattern reference)
  </read_first>
  <action>
Create exactly the following 3 YAML files:

**configs/web-research/playwright-container.yaml**
```yaml
name: "Playwright MCP (containerized)"
description: "Browser automation and web scraping via Playwright. Runs in a container with system browser dependencies installed."
# Demonstrates: container.system_deps for OS-level packages (chromium)
# No secrets required for basic use

playwright-container:
  type: stdio
  command: npx
  args:
    - "-y"
    - "@playwright/mcp"
  container:
    enabled: true
    network: bridge
    system_deps:
      - "chromium"
      - "chromium-chromedriver"
      - "dbus"
      - "dbus-x11"
      - "xvfb"
```

**configs/web-research/exa-container.yaml**
```yaml
name: "Exa Search (containerized)"
description: "Semantic web search powered by Exa AI. Runs in a container with outbound network access."
# Secrets: EXA_API_KEY — set with: argus-mcp secrets set EXA_API_KEY <your-exa-api-key>
# Get key at: https://dashboard.exa.ai/api-keys

exa-container:
  type: stdio
  command: npx
  args:
    - "-y"
    - "exa-mcp-server"
  env:
    EXA_API_KEY: "${EXA_API_KEY}"
  container:
    enabled: true
    network: bridge
```

**configs/web-research/exa.yaml**
```yaml
name: "Exa Search"
description: "Semantic web search powered by Exa AI. Runs as a subprocess."
# Secrets: EXA_API_KEY — set with: argus-mcp secrets set EXA_API_KEY <your-exa-api-key>
# Get key at: https://dashboard.exa.ai/api-keys

exa:
  type: stdio
  command: npx
  args:
    - "-y"
    - "exa-mcp-server"
  env:
    EXA_API_KEY: "${EXA_API_KEY}"
```
  </action>
  <verify>
    <automated>cd /home/diaz/mygit/argus-mcp-catalog && for f in configs/web-research/playwright-container.yaml configs/web-research/exa-container.yaml configs/web-research/exa.yaml; do echo "=== $f ===" && node -e "const y=require('js-yaml'); y.load(require('fs').readFileSync('$f','utf8')); console.log('OK')"; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep "system_deps:" /home/diaz/mygit/argus-mcp-catalog/configs/web-research/playwright-container.yaml` returns a match
    - `grep "chromium" /home/diaz/mygit/argus-mcp-catalog/configs/web-research/playwright-container.yaml` returns a match
    - `grep "EXA_API_KEY" /home/diaz/mygit/argus-mcp-catalog/configs/web-research/exa-container.yaml` returns a match
    - `grep "playwright-container:" /home/diaz/mygit/argus-mcp-catalog/configs/web-research/playwright-container.yaml` returns a match
    - `grep "exa-container:" /home/diaz/mygit/argus-mcp-catalog/configs/web-research/exa-container.yaml` returns a match
    - `grep "exa:" /home/diaz/mygit/argus-mcp-catalog/configs/web-research/exa.yaml` returns a match
    - All 3 files exist and are non-empty
  </acceptance_criteria>
  <done>3 web-research YAML files created: playwright with system_deps for chromium, exa container and subprocess variants with ${EXA_API_KEY} secrets</done>
</task>

</tasks>

<verification>
After both tasks complete, verify all 11 files exist with correct patterns:
```bash
ls /home/diaz/mygit/argus-mcp-catalog/configs/databases/
ls /home/diaz/mygit/argus-mcp-catalog/configs/web-research/
grep -l "system_deps:" /home/diaz/mygit/argus-mcp-catalog/configs/web-research/
grep -l "volumes:" /home/diaz/mygit/argus-mcp-catalog/configs/databases/
```

Note: catalog.json is NOT updated in this plan — that is handled by plan 05-06 (Wave 2) to avoid conflicts with other parallel Wave 1 plans.
</verification>

<success_criteria>
- 11 YAML files exist across databases/ and web-research/ directories
- sqlite-container.yaml has container.volumes and network: none (per D-05, D-06)
- mysql/redis/mongodb/neon container entries have bridge network and ${SECRET_NAME} env vars (per CAT-EXP-07)
- playwright-container.yaml has container.system_deps with chromium packages (per D-09)
- All files have correct backend slug matching filename stem (per CONTRIBUTING.md)
- No hardcoded secrets — all use ${SECRET_NAME} syntax
</success_criteria>

<output>
After completion, create `/home/diaz/mygit/argus-mcp-docs/.planning/phases/05-catalog-expansion/05-01-SUMMARY.md`
</output>
