---
phase: 05-catalog-expansion
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx
autonomous: true
requirements:
  - CAT-EXP-06

must_haves:
  truths:
    - "container-isolation/index.mdx documents all 14 ContainerConfig fields including the 11 previously undocumented ones"
    - "source_url pattern has its own named subsection with build_steps, entrypoint, build_env, source_ref"
    - "Go transport pattern has its own named subsection with transport and go_package"
    - "Custom Dockerfile pattern has its own named subsection with dockerfile"
    - "Basic resource limits section covers runtime, memory, cpus"
    - "Networking and mounts section covers volumes and extra_args"
    - "Each field documented with type, default, and YAML example"
  artifacts:
    - path: "/home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx"
      provides: "Expanded container-isolation page with all advanced ContainerConfig fields"
      contains: "source_url"
    - path: "/home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx"
      provides: "Go transport section"
      contains: "go_package"
    - path: "/home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx"
      provides: "Custom Dockerfile section"
      contains: "dockerfile:"
  key_links:
    - from: "container-isolation/index.mdx source_url section"
      to: "source_url, build_steps, entrypoint, build_env, source_ref fields"
      via: "New ## Source Build section appended to existing page"
      pattern: "source_url"
    - from: "container-isolation/index.mdx go transport section"
      to: "transport, go_package fields"
      via: "New ## Go Transport section appended to existing page"
      pattern: "go_package"
---

<objective>
Expand container-isolation/index.mdx to document all 14 ContainerConfig fields — the 11 currently undocumented plus rounding out the 3 partially documented ones.

Purpose: The existing page documents only 4 fields (enabled, builder_image, system_deps, additional_packages). Per D-22, D-23, D-24, D-25, CAT-EXP-06, the page must be expanded with sections for basic resource limits, networking/mounts, source builds, Go transport, and custom Dockerfiles.
Output: Extended container-isolation/index.mdx (appended to existing content, no deletions)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-catalog-expansion/05-CONTEXT.md
@.planning/phases/05-catalog-expansion/05-RESEARCH.md

<interfaces>
<!-- ContainerConfig fields from RESEARCH.md ContainerConfig Field Inventory -->

All 14 fields to document:
| Field              | Type                              | Default          | Status         |
|--------------------|-----------------------------------|------------------|----------------|
| enabled            | bool                              | true             | Documented     |
| runtime            | "docker"|"podman"|"kubernetes"|null | null (auto)    | NOT documented |
| network            | string|null                       | null (=bridge)   | Partial        |
| memory             | string|null                       | null (=512m)     | NOT documented |
| cpus               | string|null                       | null (=1)        | NOT documented |
| volumes            | List[str]                         | []               | NOT documented |
| extra_args         | List[str]                         | []               | NOT documented |
| system_deps        | List[str]                         | []               | Documented     |
| build_system_deps  | List[str]                         | []               | NOT documented |
| builder_image      | string|null                       | null (transport) | Documented     |
| additional_packages| List[str]                         | []               | Documented     |
| transport          | "uvx"|"npx"|"go"|null             | null (auto)      | NOT documented |
| go_package         | string|null                       | null             | NOT documented |
| source_url         | string|null                       | null             | NOT documented |
| build_steps        | List[str]                         | []               | NOT documented |
| entrypoint         | List[str]|null                    | null             | NOT documented |
| build_env          | Dict[str,str]                     | {}               | NOT documented |
| source_ref         | string|null                       | null             | NOT documented |
| dockerfile         | string|null                       | null             | NOT documented |

Existing page content ends at line 88 after "Pre-Building Images" section.
All additions go AFTER the existing content — no modifications to existing sections.

Groupings (per D-22, D-23, D-24 from CONTEXT.md):
1. Basic resource limits: runtime, memory, cpus (+ expand existing network description)
2. Networking and mounts: volumes, extra_args
3. Build customization: build_system_deps (distinguish from system_deps)
4. Source build pattern (subsection): source_url, build_steps, entrypoint, build_env, source_ref
5. Go transport pattern (subsection): transport, go_package
6. Custom Dockerfile pattern (subsection): dockerfile
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand container-isolation/index.mdx with all undocumented ContainerConfig fields</name>
  <files>
    /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx
  </files>
  <read_first>
    - /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx (FULL current content — read before any edits)
    - /home/diaz/mygit/argus-mcp/argus_mcp/config/schema_backends.py (ContainerConfig Pydantic model — field types, validators, docstrings)
    - /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/secrets-management/index.mdx (secrets management patterns to reference for ${SECRET_NAME} examples)
  </read_first>
  <action>
Read the existing file first (it ends at line ~88). Then APPEND the following new sections after the last line of the existing content (after the "Pre-Building Images" section). Do NOT modify any existing content — only add new sections below.

Append exactly the following markdown content to the end of the file:

---

## Resource Limits and Runtime

Override the default resource constraints or choose a specific container runtime:

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `runtime` | `"docker" \| "podman" \| "kubernetes" \| null` | `null` (auto-detect) | Container runtime to use. When `null`, Argus detects Docker or Podman automatically. |
| `memory` | `string \| null` | `null` (`512m`) | Memory limit for the container. Uses Docker memory syntax: `512m`, `1g`, `2048m`. |
| `cpus` | `string \| null` | `null` (`1`) | CPU limit for the container. Fractional values allowed: `0.5`, `1`, `2`. |
| `network` | `string \| null` | `null` (`bridge`) | Network mode. Use `none` to fully isolate the container; `bridge` to allow outbound internet access. |

```yaml
backends:
  my-backend:
    type: stdio
    command: npx
    args: ["-y", "my-mcp-server"]
    container:
      runtime: "docker"      # explicitly use Docker, not auto-detect
      memory: "1g"           # increase for memory-intensive servers
      cpus: "2"              # allow 2 CPUs
      network: "none"        # fully isolate from network
```

## Volume Mounts and Extra Arguments

Mount host directories into the container or pass extra Docker flags:

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `volumes` | `List[str]` | `[]` | Volume mount specifications in Docker format: `host_path:container_path:mode`. Mode is `ro` (read-only) or `rw` (read-write). Supports `${HOME}` and other env var expansions. |
| `extra_args` | `List[str]` | `[]` | Additional arguments passed directly to the `docker run` command, inserted before the image name. Use for flags not covered by other fields (e.g., `--device`, `--cap-add`). |

```yaml
backends:
  sqlite-db:
    type: stdio
    command: uvx
    args:
      - "mcp-server-sqlite"
      - "--db-path"
      - "/data/db.sqlite"
    container:
      network: none
      volumes:
        - "${HOME}/.mcp-data:/data:rw"   # persist database file on host

  git-server:
    type: stdio
    command: uvx
    args: ["mcp-server-git", "--repository", "/workspace"]
    container:
      network: none
      volumes:
        - "${HOME}/projects:/workspace:ro"   # read-only workspace mount
      extra_args:
        - "--device=/dev/fuse"               # pass host device into container
```

## Build Customization

### system_deps vs. build_system_deps

Both fields install OS packages, but at different stages:

| Field | Stage | Included in Final Image? | Use For |
|-------|-------|--------------------------|---------|
| `system_deps` | Runtime image | Yes | Packages needed at runtime (e.g., `chromium`, `git`, `curl`) |
| `build_system_deps` | Builder stage only | No | Packages needed only during build (e.g., `gcc`, `make`, `cmake`) |

```yaml
backends:
  my-compiled-server:
    type: stdio
    command: python
    args: ["-m", "my_server"]
    container:
      system_deps:
        - "libssl3"         # needed at runtime
      build_system_deps:
        - "gcc"             # needed to compile C extension during build
        - "python3-dev"     # needed for build only
```

## Source Build Pattern

For MCP servers published only on GitHub — with no npm or PyPI package — Argus can clone and build from source at image build time.

**Required fields when using `source_url`:** `build_steps` and `entrypoint` are both required. Omitting either will cause a validation error.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `source_url` | `string \| null` | `null` | Git repository URL to clone. Must use `https://` or `git+ssh://`. Private/loopback IPs are rejected. |
| `build_steps` | `List[str]` | `[]` | Commands to run after cloning the repository. Each step is a separate string. **Cannot contain backticks, `$()`, or `()`** — use separate list entries instead of shell chaining. |
| `entrypoint` | `List[str] \| null` | `null` | Container entrypoint command as a list. Set to the command that starts the MCP server. **Cannot contain shell metacharacters** (`;`, `&`, `|`, backtick, `$`, `(`, `)`, `{`, `}`, `[`, `]`, `<`, `>`, `!`, `#`, `~`, `\`). |
| `build_env` | `Dict[str, str]` | `{}` | Environment variables available only during the build stage. Not present in the final runtime image. |
| `source_ref` | `string \| null` | `null` | Git ref (branch, tag, or commit SHA) to check out. When `null`, clones the default branch. |

**Base image selection:** If any build step or entrypoint element contains `npm`, `npx`, `node`, `yarn`, or `pnpm`, Argus uses `node:22-alpine`. Otherwise it uses `python:3.13-slim`.

```yaml
backends:
  my-github-server:
    type: stdio
    command: node
    args: ["dist/index.js"]
    container:
      network: bridge
      source_url: https://github.com/owner/my-mcp-server.git
      source_ref: "v1.2.0"          # pin to a release tag
      build_steps:
        - "npm install"
        - "npm run build"            # each step is a separate list entry
      entrypoint:
        - "node"
        - "dist/index.js"
      build_env:
        NODE_ENV: "production"       # only set during build
```

<Note type="warning">
Shell expansion syntax is not supported in `build_steps`. Do not use backticks, `$()`, or `()`. Split compound operations into separate list entries:

```yaml
# Wrong — validator will reject this:
build_steps:
  - "VERSION=$(cat package.json | grep version)"

# Correct — use separate steps:
build_steps:
  - "npm install"
  - "npm run build"
```
</Note>

## Go Transport Pattern

For MCP servers written in Go and published as Go modules, Argus can compile the binary from the module path using `go install`.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `transport` | `"uvx" \| "npx" \| "go" \| null` | `null` (auto-detect from command) | Override transport detection. Set to `"go"` to use the Go build pipeline. The compiled binary is placed at `/app/mcp-server` in the final image. |
| `go_package` | `string \| null` | `null` | Go module path passed to `go install`. Required when `transport: go`. Example: `github.com/strowk/mcp-k8s-go`. |

```yaml
backends:
  kubernetes:
    type: stdio
    command: mcp-k8s     # binary name after go install
    container:
      transport: go
      go_package: github.com/strowk/mcp-k8s-go
      network: bridge
      volumes:
        - "${HOME}/.kube:/home/nonroot/.kube:ro"   # kubeconfig mount
```

When `transport: go` is set, Argus uses a `golang:1.24-alpine` builder stage and places the compiled binary at `/app/mcp-server`. The `command` field in the backend config should match the binary's installed name.

## Custom Dockerfile Pattern

For servers with build requirements too complex for the auto-generated templates, you can provide a custom Dockerfile.

| Field | Type | Default | Description |
|-------|------|---------|-------------|
| `dockerfile` | `string \| null` | `null` | Relative path to a custom Dockerfile. **Must be a relative path with no `..` components.** The Dockerfile must be in the same directory as (or a subdirectory of) the backend config YAML file. |

**Dockerfile placement:** The validator rejects absolute paths and any path containing `..`. The recommended location is alongside the config YAML file, with the filename as `container.dockerfile: {server-slug}.dockerfile`.

```yaml
backends:
  my-custom-server:
    type: stdio
    command: python
    args: ["-m", "my_server"]
    container:
      dockerfile: my-custom-server.dockerfile   # co-located with this YAML
      network: none
      volumes:
        - "${HOME}/data:/data:ro"
```

The Dockerfile resides at `configs/{category}/my-custom-server.dockerfile` — the same directory as the YAML config.

<Note type="info">
Use the `dockerfile` escape hatch only when standard ContainerConfig fields cannot express your requirements. For most servers, `source_url` (for GitHub-only builds), `go_package` (for Go binaries), `system_deps` (for extra OS packages), and `builder_image` (for base image overrides) cover the common cases without requiring a custom Dockerfile.
</Note>
  </action>
  <verify>
    <automated>grep -c "source_url" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx && grep -c "go_package" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx && grep -c "dockerfile" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "source_url" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns at least 2 matches (field table + YAML example)
    - `grep "go_package" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns at least 2 matches
    - `grep "build_steps" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns at least 2 matches
    - `grep "entrypoint" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns at least 2 matches
    - `grep "dockerfile" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns at least 2 matches
    - `grep "volumes" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns at least 2 matches
    - `grep "build_system_deps" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns at least 1 match
    - `grep "## Source Build Pattern" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns a match
    - `grep "## Go Transport Pattern" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns a match
    - `grep "## Custom Dockerfile Pattern" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns a match
    - Original content still present: `grep "## Image Building Pipeline" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns a match
    - Original content still present: `grep "## Per-Backend Container Options" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx` returns a match
  </acceptance_criteria>
  <done>container-isolation/index.mdx expanded with 6 new sections covering all 11 undocumented ContainerConfig fields, plus expanded documentation for network field (CAT-EXP-06)</done>
</task>

</tasks>

<verification>
After task completes, verify no existing sections were removed and all new fields are present:
```bash
grep "##" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx
wc -l /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/container-isolation/index.mdx
```

The file should have 7+ sections (original 4 + 6 new) and be significantly longer than the original ~88 lines.
</verification>

<success_criteria>
- All 11 undocumented ContainerConfig fields now have documentation in container-isolation/index.mdx
- source_url, build_steps, entrypoint, build_env, source_ref covered in "Source Build Pattern" subsection
- transport, go_package covered in "Go Transport Pattern" subsection
- dockerfile covered in "Custom Dockerfile Pattern" subsection
- runtime, memory, cpus, volumes, extra_args, build_system_deps covered in their respective sections
- No existing content removed — only additions (per D-25)
- Each field has type, default, description, and YAML example
</success_criteria>

<output>
After completion, create `/home/diaz/mygit/argus-mcp-docs/.planning/phases/05-catalog-expansion/05-05-SUMMARY.md`
</output>
