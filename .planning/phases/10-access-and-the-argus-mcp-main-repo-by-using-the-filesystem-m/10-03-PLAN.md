---
phase: "10-access-and-the-argus-mcp-main-repo-by-using-the-filesystem-m"
plan: "03"
type: execute
wave: 2
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - settings/documents.ts
autonomous: true
requirements:
  - D-11
  - D-12
  - D-13

must_haves:
  truths:
    - "The docs site nav includes a Security Configuration entry under the Configuration section"
    - "The docs site nav includes a Rate Limits entry under the Configuration section"
    - "pnpm build completes without errors after all Phase 10 edits"
  artifacts:
    - path: "settings/documents.ts"
      provides: "Nav entries for security and rate-limits pages"
      contains: "configuration/security"
  key_links:
    - from: "settings/documents.ts"
      to: "contents/docs/configuration/security/index.mdx"
      via: "nav href pointing to the new security page (created in Plan 01)"
      pattern: "configuration/security"
    - from: "settings/documents.ts"
      to: "contents/docs/configuration/rate-limits/index.mdx"
      via: "nav href pointing to the new rate-limits page (created in Plan 01)"
      pattern: "configuration/rate-limits"
---

<objective>
Wire the two new configuration pages (security + rate-limits) into the site navigation, then run `pnpm build` as the final phase gate to confirm all MDX edits from Plans 01 and 02 compile cleanly.

Purpose: New pages are unreachable without nav entries. The build gate confirms no MDX syntax errors were introduced across all three plans.

Output: Updated `settings/documents.ts` with two new nav entries; clean `pnpm build`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-access-and-the-argus-mcp-main-repo-by-using-the-filesystem-m/10-CONTEXT.md
@settings/documents.ts
@.planning/phases/10-access-and-the-argus-mcp-main-repo-by-using-the-filesystem-m/10-01-SUMMARY.md
@.planning/phases/10-access-and-the-argus-mcp-main-repo-by-using-the-filesystem-m/10-02-SUMMARY.md
</context>

<interfaces>
<!-- Existing nav pattern in settings/documents.ts — match exactly -->
<!-- Configuration section already contains entries like: -->
<!--   { title: "Session Pool", href: "/docs/configuration/session-pool" } -->
<!--   { title: "HTTP Pool", href: "/docs/configuration/http-pool" } -->
<!--   { title: "Authentication", href: "/docs/configuration/authentication" } -->

New entries to add (match same object shape):
  { title: "Security", href: "/docs/configuration/security" }
  { title: "Rate Limits", href: "/docs/configuration/rate-limits" }

These must be added within the Configuration section grouping, after "Authentication" and before any non-configuration entries.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Security and Rate Limits nav entries to documents.ts</name>
  <files>settings/documents.ts</files>
  <action>
Read `settings/documents.ts` to locate the Configuration section in the navigation structure.

Find the existing Authentication entry:
```
{ title: "Authentication", href: "/docs/configuration/authentication" }
```

Insert the two new entries immediately after it, using the same object shape:
```
{ title: "Security", href: "/docs/configuration/security" },
{ title: "Rate Limits", href: "/docs/configuration/rate-limits" },
```

Use the Edit tool with `oldString` / `newString`. Do not change any other nav entries, ordering, or formatting.

If the file uses a different object shape (e.g., `path` instead of `href`, or includes `icon`/`description` fields), match the existing shape exactly — read the file first to confirm the pattern before writing.
  </action>
  <verify>
    <automated>grep -q "configuration/security" settings/documents.ts && grep -q "configuration/rate-limits" settings/documents.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both nav entries present in settings/documents.ts with correct href values matching the new page paths.</done>
</task>

<task type="auto">
  <name>Task 2: Run pnpm build — phase gate</name>
  <files></files>
  <action>
Run `pnpm build` from the project root. This is the final gate for the entire phase.

If the build succeeds: phase is complete.

If the build fails with an MDX parse error:
1. Read the error output to identify which file and line failed
2. Fix only the syntax error (do not alter content beyond the fix)
3. Re-run `pnpm build`
4. If still failing after one fix attempt, note the specific file as `BLOCKED` (per D-13) and revert only that file to its pre-phase state using `git checkout -- <file>`

Do not accept a failing build. Either fix or revert — the build must pass.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>pnpm build exits with code 0. No errors in output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nav config → rendered site | Broken href silently creates dead links |
| MDX → Next.js build | Syntax errors in any MDX file fail the entire build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | settings/documents.ts nav hrefs | mitigate | Verify href paths match actual file paths; pnpm build will 404-check routes |
| T-10-04 | Denial of Service | pnpm build gate | mitigate | Build must pass; revert-on-failure rule in D-13 prevents broken deploy |
</threat_model>

<verification>
`grep -q "configuration/security" settings/documents.ts && grep -q "configuration/rate-limits" settings/documents.ts && pnpm build` — all pass.
</verification>

<success_criteria>
- `settings/documents.ts` contains nav entries for `/docs/configuration/security` and `/docs/configuration/rate-limits`
- `pnpm build` exits 0 with no errors
- All four drift findings from RESEARCH.md are now closed (two new pages + two edits to existing pages)
</success_criteria>

<output>
After completion, create `.planning/phases/10-access-and-the-argus-mcp-main-repo-by-using-the-filesystem-m/10-03-SUMMARY.md`
</output>
