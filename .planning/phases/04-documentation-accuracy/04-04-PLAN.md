---
phase: 04-documentation-accuracy
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - contents/docs/skills/overview/index.mdx
  - contents/docs/workflows/overview/index.mdx
autonomous: true
requirements: [DOC-03]
must_haves:
  truths:
    - "Skills overview has a complete manifest.json field reference (name, version, description, tools, workflows, config, dependencies, author, license)"
    - "Skills overview documents the enable/disable lifecycle via argus skills enable/disable CLI commands"
    - "Workflows overview has a complete workflow YAML field reference (name, steps with run/input/depends fields)"
    - "Workflows overview documents how steps declare dependencies and how parallel execution works"
    - "No speculative content — all fields derive from manifest.py, manager.py, dsl.py, steps.py"
  artifacts:
    - path: "contents/docs/skills/overview/index.mdx"
      provides: "Full skills reference — manifest format, lifecycle, config, directory layout"
      min_lines: 150
    - path: "contents/docs/workflows/overview/index.mdx"
      provides: "Full workflows reference — YAML format, DAG engine, step types, examples"
      min_lines: 150
  key_links:
    - from: "contents/docs/skills/overview/index.mdx"
      to: "argus_mcp/skills/manifest.py SkillManifest"
      via: "Field names match SkillManifest dataclass fields"
      pattern: "dependencies|tools|workflows|config"
    - from: "contents/docs/workflows/overview/index.mdx"
      to: "argus_mcp/workflows/dsl.py"
      via: "Workflow YAML field names match DSL model fields"
      pattern: "steps|depends|run|input"
---

<objective>
Expand the Skills and Workflows overview pages to full reference depth. Both are currently placeholder pages (one bullet pointing to the overview). Update each overview in-place with complete field reference, examples, and usage guide derived entirely from source code.

Purpose: Users configuring Skills and Workflows need complete documentation — not a stub (per D-13, D-14, D-15).
Output: Updated contents/docs/skills/overview/index.mdx and contents/docs/workflows/overview/index.mdx with reference-depth content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-documentation-accuracy/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Skills overview to full reference depth</name>
  <files>contents/docs/skills/overview/index.mdx</files>
  <read_first>
    - /home/diaz/mygit/argus-mcp/argus_mcp/skills/manifest.py — SkillManifest dataclass: all fields (name, version, description, tools, workflows, config, dependencies, author, license), from_dict parsing, validation logic
    - /home/diaz/mygit/argus-mcp/argus_mcp/skills/manager.py — SkillManager: __init__ (skills_dir), discover(), install/enable/disable/uninstall API, skills-state.json layout, InstalledSkill, SkillStatus enum
    - contents/docs/skills/overview/index.mdx — read current content (269 lines) to understand what's already there and what to add/expand
  </read_first>
  <action>
    The current skills/overview/index.mdx is 269 lines. Read it fully before making changes. Expand it in-place (do not replace — add to it) to reach reference depth matching backends/index.mdx.

    Sections to add or expand (only if not already present):

    1. **Directory Layout** — Show the expected directory structure:
    ```
    skills/
      my-skill/
        manifest.json
        [additional files referenced by manifest]
    skills-state.json   (auto-generated by SkillManager)
    ```

    2. **manifest.json Reference** — Complete field table derived from SkillManifest dataclass:
    | Field | Type | Required | Description |
    |-------|------|----------|-------------|
    | `name` | string | yes | Unique skill identifier |
    | `version` | string | no (default "0.0.0") | Semantic version string |
    | `description` | string | no | Human-readable description |
    | `tools` | array | no | Tool definitions this skill provides |
    | `workflows` | array | no | Workflow definitions this skill provides |
    | `config` | object | no | Default configuration for this skill (namespaced) |
    | `dependencies` | array | no | Other skill names this skill depends on |
    | `author` | string | no | Skill author |
    | `license` | string | no | License identifier |

    Include a complete manifest.json example.

    3. **Lifecycle: Enable/Disable** — Document that SkillManager tracks state in skills-state.json. Skills default to enabled on discovery. Use the management API (/manage/v1/skills/{name}/enable|disable) or the TUI to toggle.

    4. **Configuration** — The `config` key in manifest.json provides default config values that are namespaced under the skill name. The `skills:` section in config.yaml controls discovery directory and enable/disable.

    Do NOT fabricate capabilities not in the source. If the current overview already covers a section well, leave it unchanged. Only add what is missing or sparse.
  </action>
  <verify>
    <automated>wc -l /home/diaz/mygit/argus-mcp-docs/contents/docs/skills/overview/index.mdx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "manifest.json" contents/docs/skills/overview/index.mdx` — returns match
    - `grep "dependencies" contents/docs/skills/overview/index.mdx` — returns match (SkillManifest field)
    - `grep "skills-state.json" contents/docs/skills/overview/index.mdx` — returns match (SkillManager state file)
    - `grep "enable\|disable" contents/docs/skills/overview/index.mdx` — returns match (lifecycle section)
    - `wc -l contents/docs/skills/overview/index.mdx` — output is >= 200 lines (ensuring reference depth added)
    - File starts with valid MDX frontmatter (`grep "^---" contents/docs/skills/overview/index.mdx` returns 2 matches)
  </acceptance_criteria>
  <done>Skills overview contains complete manifest.json field reference, directory layout, and lifecycle documentation derived from manifest.py and manager.py.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Workflows overview to full reference depth</name>
  <files>contents/docs/workflows/overview/index.mdx</files>
  <read_first>
    - /home/diaz/mygit/argus-mcp/argus_mcp/workflows/dsl.py — workflow YAML DSL model definitions: all fields for workflow definition, step definitions, input/output mappings
    - /home/diaz/mygit/argus-mcp/argus_mcp/workflows/steps.py — step type implementations: what step types exist (e.g., tool call steps, transform steps), their fields
    - /home/diaz/mygit/argus-mcp/argus_mcp/workflows/executor.py — DAG execution: how dependencies are resolved, parallel execution behavior, error handling
    - /home/diaz/mygit/argus-mcp/argus_mcp/workflows/composite_tool.py — how workflows are exposed as MCP tools
    - contents/docs/workflows/overview/index.mdx — read current content (303 lines) fully before modifying
  </read_first>
  <action>
    The current workflows/overview/index.mdx is 303 lines. Read it fully before making changes. Expand it in-place to reach reference depth.

    Sections to add or expand (only if not already present):

    1. **Workflow YAML Format** — Complete field reference for a workflow YAML file. Derive field names and structure from dsl.py. Typical fields: name, description, steps (array).

    2. **Step Reference** — Document step types and their fields derived from steps.py and dsl.py. For each step type: name, what it does, required fields, optional fields.

    3. **Dependency Resolution** — Explain how `depends:` or equivalent dependency fields work. Steps without dependencies run in parallel (Wave 1). Steps that depend on other steps run after their dependencies complete. Derive from executor.py.

    4. **Input/Output Mapping** — How steps pass outputs to dependent steps. Derive from dsl.py output/input mapping fields.

    5. **Workflow as MCP Tool** — How the workflow is exposed to MCP clients as a single tool (derived from composite_tool.py).

    6. **Complete Example** — A full workflow YAML with 3+ steps showing parallel and sequential execution.

    Constraint: derive all field names and behavior from the source files listed above. If source files show that workflows have simpler structure than described in the current docs, correct the docs to match the code.
  </action>
  <verify>
    <automated>wc -l /home/diaz/mygit/argus-mcp-docs/contents/docs/workflows/overview/index.mdx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "steps:" contents/docs/workflows/overview/index.mdx` — returns match (step array field)
    - `grep "depends\|parallel" contents/docs/workflows/overview/index.mdx` — returns match (dependency/DAG section)
    - `wc -l contents/docs/workflows/overview/index.mdx` — output is >= 200 lines
    - File contains at least one ```yaml block with a multi-step workflow example
    - File starts with valid MDX frontmatter (`grep "^---"` returns 2 matches)
    - No field names appear that don't exist in dsl.py or steps.py (verify against source during implementation)
  </acceptance_criteria>
  <done>Workflows overview contains complete step field reference, DAG execution explanation, and input/output mapping documentation derived from dsl.py, steps.py, and executor.py.</done>
</task>

</tasks>

<verification>
Both files updated with reference-depth content:
- `wc -l contents/docs/skills/overview/index.mdx` >= 200
- `wc -l contents/docs/workflows/overview/index.mdx` >= 200
- Key source-derived field names present in each file
</verification>

<success_criteria>
- Skills overview: complete manifest.json field table, directory layout, lifecycle guide — all derived from manifest.py and manager.py
- Workflows overview: complete step reference, DAG dependency model, parallel execution behavior — all derived from dsl.py, steps.py, executor.py
- No speculative content in either file
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-accuracy/04-04-SUMMARY.md`
</output>
