---
phase: 04-documentation-accuracy
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - contents/docs/optimizer/overview/index.mdx
  - contents/docs/registry/overview/index.mdx
  - contents/docs/tui/overview/index.mdx
autonomous: true
requirements: [DOC-03]
must_haves:
  truths:
    - "Optimizer overview documents both meta-tools (find_tool, call_tool) with their input schemas derived from meta_tools.py"
    - "Optimizer overview documents the optimizer config fields (enabled, keep_tools) and when to use the optimizer"
    - "Registry overview documents the registries: config array with RegistryEntryConfig fields derived from schema_registry.py"
    - "Registry overview documents the GET /registry/search API usage and how the TUI uses it"
    - "TUI overview documents all major screens visible from app.py/screens/ with their purpose and key bindings"
    - "No speculative content — all details derive from the source files listed in read_first"
  artifacts:
    - path: "contents/docs/optimizer/overview/index.mdx"
      provides: "Optimizer reference — meta-tool schemas, keep_tools config, when to enable"
      min_lines: 100
    - path: "contents/docs/registry/overview/index.mdx"
      provides: "Registry reference — registries config, search API, caching, TUI usage"
      min_lines: 150
    - path: "contents/docs/tui/overview/index.mdx"
      provides: "TUI reference — screens, keybindings, configuration"
      min_lines: 200
  key_links:
    - from: "contents/docs/optimizer/overview/index.mdx"
      to: "argus_mcp/bridge/optimizer/meta_tools.py"
      via: "find_tool and call_tool input schema field names"
      pattern: "find_tool|call_tool|keep_tools"
    - from: "contents/docs/registry/overview/index.mdx"
      to: "argus_mcp/config/schema_registry.py RegistryEntryConfig"
      via: "registries array field names"
      pattern: "url|provider|cache_ttl"
    - from: "contents/docs/tui/overview/index.mdx"
      to: "argus_mcp/tui/app.py and screens/"
      via: "Screen names and keybindings from source"
      pattern: "BINDINGS|Screen"
---

<objective>
Expand three sparse feature section overviews (Optimizer, Registry, TUI) to full reference depth. All content derives from the actual source code — no speculation.

Purpose: Users of these features need complete configuration reference and usage guide (per D-13, D-14, D-15).
Output: Updated optimizer/overview/index.mdx, registry/overview/index.mdx, and tui/overview/index.mdx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-documentation-accuracy/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand Optimizer overview to full reference depth</name>
  <files>contents/docs/optimizer/overview/index.mdx</files>
  <read_first>
    - /home/diaz/mygit/argus-mcp/argus_mcp/bridge/optimizer/meta_tools.py — find_tool and call_tool definitions: input parameter names, types, descriptions; what each tool returns
    - /home/diaz/mygit/argus-mcp/argus_mcp/bridge/optimizer/search.py — how tool search works: indexing, query matching behavior
    - /home/diaz/mygit/argus-mcp/argus_mcp/config/schema.py OptimizerConfig — enabled (bool, default false), keep_tools (list[str], default [])
    - contents/docs/optimizer/overview/index.mdx — read current 155 lines before modifying
  </read_first>
  <action>
    The current optimizer/overview/index.mdx is 155 lines. Read it fully before modifying. Expand it in-place.

    Sections to add or expand:

    1. **find_tool reference** — Derive from meta_tools.py. Document the exact input parameters (name/pattern/query field, any filter params), what the tool returns (list of matching tool names with descriptions), and usage example showing an LLM calling find_tool.

    2. **call_tool reference** — Derive from meta_tools.py. Document exact input params (tool_name, arguments), what it returns, and how it routes to the correct backend.

    3. **keep_tools configuration** — The `optimizer.keep_tools` list lets you always expose specific tools alongside the meta-tools. Useful for tools the LLM always needs (e.g., a search tool). Example config showing keep_tools usage.

    4. **When to enable** — Clear guidance: enable when a server exposes 100+ tools and LLM context is expensive. Do not enable for small tool catalogs (overhead not worth it). This is a high-risk feature flag that is off by default.

    5. **Configuration reference** — Full optimizer: section in config.yaml with both fields:
    ```yaml
    optimizer:
      enabled: true
      keep_tools:
        - search_web
        - read_file
    ```

    Constraint: derive find_tool and call_tool input schemas from meta_tools.py only. Do not invent parameter names.
  </action>
  <verify>
    <automated>grep -c "find_tool\|call_tool" /home/diaz/mygit/argus-mcp-docs/contents/docs/optimizer/overview/index.mdx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "find_tool" contents/docs/optimizer/overview/index.mdx` — returns match (meta-tool documented)
    - `grep "call_tool" contents/docs/optimizer/overview/index.mdx` — returns match (meta-tool documented)
    - `grep "keep_tools" contents/docs/optimizer/overview/index.mdx` — returns match (config field documented)
    - `grep "enabled.*false\|default.*off\|high-risk\|opt-in" contents/docs/optimizer/overview/index.mdx` — returns match (safety warning present)
    - `wc -l contents/docs/optimizer/overview/index.mdx` — output >= 120 lines
    - File contains at least one ```yaml block showing optimizer config
  </acceptance_criteria>
  <done>Optimizer overview documents both meta-tools with input schemas, keep_tools config, and when-to-enable guidance — all derived from source.</done>
</task>

<task type="auto">
  <name>Task 2: Expand Registry and TUI overviews to full reference depth</name>
  <files>contents/docs/registry/overview/index.mdx, contents/docs/tui/overview/index.mdx</files>
  <read_first>
    - /home/diaz/mygit/argus-mcp/argus_mcp/config/schema_registry.py — RegistryEntryConfig: all fields with types/defaults/descriptions
    - /home/diaz/mygit/argus-mcp/argus_mcp/registry/catalog.py — catalog fetching: how remote registry URLs are fetched, cache behavior
    - /home/diaz/mygit/argus-mcp/argus_mcp/registry/cache.py — cache implementation: TTL, invalidation
    - /home/diaz/mygit/argus-mcp/argus_mcp/registry/client.py — registry client: search, install flow
    - /home/diaz/mygit/argus-mcp/argus_mcp/tui/app.py — main TUI app: BINDINGS, screen definitions, key handlers
    - /home/diaz/mygit/argus-mcp/argus_mcp/tui/screens/ — list all screen files; read app.py for screen names and their BINDINGS
    - contents/docs/registry/overview/index.mdx — read current 243 lines before modifying
    - contents/docs/tui/overview/index.mdx — read current 398 lines before modifying
  </read_first>
  <action>
    Read both current overview files fully before modifying. Expand each in-place.

    **Registry overview additions:**

    1. **registries: configuration** — RegistryEntryConfig field table. Derive from schema_registry.py. Common fields: `url` (string, required), `name` (string), `provider` (string) and any other fields in RegistryEntryConfig. Show YAML example:
    ```yaml
    registries:
      - url: "https://registry.example.com"
        name: "my-registry"
    ```

    2. **Search API** — How to use GET /manage/v1/registry/search with `q` param. Show curl example.

    3. **Caching** — Derive from cache.py: how catalog responses are cached, TTL behavior, when cache is invalidated.

    4. **Install flow** — If registry client.py shows an install flow (downloading/adding backend config), document it.

    **TUI overview additions:**

    The current file is 398 lines. Read it to see what's already covered. Fill only what is sparse or missing:

    1. **Screens reference** — List all screens from app.py/screens/. For each screen: name, what it shows, how to navigate to it. Derive screen names from the screens/ directory listing.

    2. **Key bindings** — Derive BINDINGS from app.py (the `BINDINGS` class variable). Document all keyboard shortcuts with their actions.

    3. **Configuration** — The `client:` section in config.yaml controls TUI settings. Derive from schema_client.py (ClientConfig). Document relevant fields.

    Constraint: for both files, do not add content beyond what the source code actually implements. Where source is unclear, note "behavior depends on registry implementation" rather than speculating.
  </action>
  <verify>
    <automated>grep "RegistryEntryConfig\|registries:\|url:" /home/diaz/mygit/argus-mcp-docs/contents/docs/registry/overview/index.mdx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep "registries:" contents/docs/registry/overview/index.mdx` — returns match (config section present)
    - `grep "cache\|ttl\|TTL" contents/docs/registry/overview/index.mdx` — returns match (caching documented)
    - `grep "registry/search\|GET /registry" contents/docs/registry/overview/index.mdx` — returns match (search API section)
    - `grep "BINDINGS\|binding\|keyboard\|keybind" contents/docs/tui/overview/index.mdx` — returns match (keybindings section)
    - `wc -l contents/docs/registry/overview/index.mdx` — output >= 180 lines
    - `wc -l contents/docs/tui/overview/index.mdx` — output >= 300 lines (it was already 398; do not shrink it)
    - Registry file contains at least one ```yaml block showing registries: config
    - No field names in registry config section that don't exist in schema_registry.py (verify during implementation)
  </acceptance_criteria>
  <done>Registry overview documents registries: config, search API, and caching. TUI overview documents screens and key bindings. Both derived from source.</done>
</task>

</tasks>

<verification>
Final checks:
- `grep "find_tool\|call_tool\|keep_tools" contents/docs/optimizer/overview/index.mdx` — 3 matches
- `grep "registries:" contents/docs/registry/overview/index.mdx` — match
- `grep "BINDINGS\|keybind\|keyboard" contents/docs/tui/overview/index.mdx` — match
</verification>

<success_criteria>
- optimizer/overview: meta-tool input schemas, keep_tools, when-to-enable — derived from meta_tools.py and schema.py
- registry/overview: registries config fields, search API, caching — derived from schema_registry.py, cache.py
- tui/overview: screens, keybindings, config — derived from app.py and screens/
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-accuracy/04-05-SUMMARY.md`
</output>
