---
phase: 04-documentation-accuracy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contents/docs/plugins/index.mdx
  - contents/docs/plugins/built-in-plugins/index.mdx
  - contents/docs/configuration/backends/index.mdx
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "Plugin count claim says '8' nowhere — correct count (8) is still accurate but the combined example must list all 8 plugins"
    - "The built-in plugins page describes all 8 plugins that exist in argus_mcp/plugins/builtins/ — not 10+ (actual count is 8)"
    - "The backends configuration page documents all 6 timeout fields: init, cap_fetch, sse_startup, startup, retries, retry_delay"
  artifacts:
    - path: "contents/docs/plugins/index.mdx"
      provides: "Plugin section landing page with accurate count"
    - path: "contents/docs/plugins/built-in-plugins/index.mdx"
      provides: "Complete built-in plugin reference matching builtins/ directory"
    - path: "contents/docs/configuration/backends/index.mdx"
      provides: "Backend config reference with all 6 timeout fields"
  key_links:
    - from: "contents/docs/plugins/built-in-plugins/index.mdx"
      to: "argus_mcp/plugins/builtins/"
      via: "Plugin names match Python module filenames"
      pattern: "startup|retries|retry_delay"
    - from: "contents/docs/configuration/backends/index.mdx"
      to: "argus_mcp/config/schema_backends.py TimeoutConfig"
      via: "Field table rows match model fields"
      pattern: "startup.*retries.*retry_delay"
---

<objective>
Fix the two demonstrably wrong content issues in the docs site.

1. Verify actual plugin count: enumerate argus_mcp/plugins/builtins/ and confirm the "eight built-in plugins" claim is correct or wrong. Update index.mdx and built-in-plugins/index.mdx to match the actual count.
2. Add 3 missing timeout fields (startup, retries, retry_delay) to the backends configuration page.

Purpose: Code is the source of truth. Where docs and code conflict, docs are updated to match code (per D-02, D-04, D-05).
Output: Updated index.mdx (plugins), updated built-in-plugins/index.mdx, updated backends/index.mdx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-documentation-accuracy/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify plugin count and update plugins pages</name>
  <files>contents/docs/plugins/index.mdx, contents/docs/plugins/built-in-plugins/index.mdx</files>
  <read_first>
    - /home/diaz/mygit/argus-mcp/argus_mcp/plugins/builtins/ — list all .py files (excluding __init__.py and __pycache__) to get the definitive plugin count and names
    - /home/diaz/mygit/argus-mcp/argus_mcp/plugins/builtins/circuit_breaker.py — verify plugin name and settings
    - /home/diaz/mygit/argus-mcp/argus_mcp/plugins/builtins/markdown_cleaner.py — verify plugin name and settings
    - /home/diaz/mygit/argus-mcp/argus_mcp/plugins/builtins/output_length_guard.py — verify plugin name and settings
    - /home/diaz/mygit/argus-mcp/argus_mcp/plugins/builtins/pii_filter.py — verify plugin name and settings
    - /home/diaz/mygit/argus-mcp/argus_mcp/plugins/builtins/rate_limiter.py — verify plugin name and settings
    - /home/diaz/mygit/argus-mcp/argus_mcp/plugins/builtins/response_cache_by_prompt.py — verify plugin name and settings
    - /home/diaz/mygit/argus-mcp/argus_mcp/plugins/builtins/retry_with_backoff.py — verify plugin name and settings
    - /home/diaz/mygit/argus-mcp/argus_mcp/plugins/builtins/secrets_detection.py — verify plugin name and settings
    - contents/docs/plugins/index.mdx — read current content before modifying
    - contents/docs/plugins/built-in-plugins/index.mdx — read current content before modifying
  </read_first>
  <action>
    Count the .py files in argus_mcp/plugins/builtins/ (excluding __init__.py). The current docs claim "eight built-in plugins" — verify this number matches the actual count.

    The builtins/ directory contains: circuit_breaker.py, markdown_cleaner.py, output_length_guard.py, pii_filter.py, rate_limiter.py, response_cache_by_prompt.py, retry_with_backoff.py, secrets_detection.py — that is 8 files. The count is correct.

    Step 1 — Update contents/docs/plugins/index.mdx:
    - The landing page says "eight built-in plugins" — confirm this matches the actual 8 plugins and update the wording only if the count changed.
    - The "Built-in Plugins" link text should reference the correct count.

    Step 2 — Update contents/docs/plugins/built-in-plugins/index.mdx:
    - Read each builtin .py file to confirm the exact plugin name (the `name` attribute or class name used in YAML), its configurable settings with types and defaults, and what the plugin does.
    - The existing page already documents all 8 correctly. Verify each section matches the source:
      - secrets_detection: settings `block` (bool, default true)
      - pii_filter: settings `categories` (list, default [])
      - rate_limiter: settings `max_requests` (int, default 100), `window` (int, default 60)
      - circuit_breaker: settings `failure_threshold` (int, default 5), `cooldown` (int, default 30)
      - response_cache_by_prompt: verify settings from response_cache_by_prompt.py
      - retry_with_backoff: verify settings from retry_with_backoff.py
      - output_length_guard: settings `max_length` (int, default 50000)
      - markdown_cleaner: settings `strip_images` (bool, default true), `strip_links` (bool, true), `strip_html` (bool, true)
    - Fix any discrepancies found. If all settings match, no change is needed for built-in-plugins/index.mdx.
    - Update the page title/description to reflect the verified count.
  </action>
  <verify>
    <automated>grep -c "^## " /home/diaz/mygit/argus-mcp-docs/contents/docs/plugins/built-in-plugins/index.mdx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "eight built-in\|8 built-in" contents/docs/plugins/index.mdx` — returns a match confirming accurate count
    - `grep "^## " contents/docs/plugins/built-in-plugins/index.mdx | wc -l` — returns 9 (8 plugin sections + "Combined Example")
    - Each of these plugin names appears as a section header: secrets_detection, pii_filter, rate_limiter, circuit_breaker, response_cache_by_prompt, retry_with_backoff, output_length_guard, markdown_cleaner
    - No plugin section is missing its Settings table and YAML example
  </acceptance_criteria>
  <done>Plugin pages accurately reflect the 8 plugins in builtins/ with verified settings from source code.</done>
</task>

<task type="auto">
  <name>Task 2: Add 3 missing timeout fields to backends config docs</name>
  <files>contents/docs/configuration/backends/index.mdx</files>
  <read_first>
    - /home/diaz/mygit/argus-mcp/argus_mcp/config/schema_backends.py — read TimeoutConfig class (lines 49-87) for all 6 fields with their types, defaults, ge/le bounds, and descriptions
    - contents/docs/configuration/backends/index.mdx — read the full file, specifically the "### Timeouts" section (around line 125-138) to see what's currently documented
  </read_first>
  <action>
    The current "### Timeouts" section in backends/index.mdx documents only 3 fields (init, cap_fetch, sse_startup). The TimeoutConfig model in schema_backends.py has 6 fields.

    Update the Timeouts section in contents/docs/configuration/backends/index.mdx:

    1. Update the YAML example block to include all 6 fields:
    ```yaml
    timeouts:
      init: 30          # MCP session initialization (seconds)
      cap_fetch: 10     # Capability list fetch (seconds)
      sse_startup: 5    # Wait for local SSE process to start (seconds)
      startup: 60       # Overall connection timeout including subprocess spawn (seconds)
      retries: 3        # Automatic retries for failed connections
      retry_delay: 5    # Seconds between retry attempts
    ```

    2. Update the field table to add the 3 missing rows after the existing rows:
    | `startup` | float | `null` (no default) | Overall per-backend connection timeout including subprocess spawn; useful for cold-start scenarios where uvx/npx downloads packages |
    | `retries` | int | `null` (no default) | Number of automatic retries for failed backend connections (0–10) |
    | `retry_delay` | float | `null` (no default) | Seconds to wait between retry attempts (0–120) |

    All values from TimeoutConfig in schema_backends.py:
    - startup: Optional[float], ge=0, "Overall per-backend connection timeout in seconds (covers subprocess spawn + MCP init). Useful for cold-start scenarios where uvx/npx downloads packages."
    - retries: Optional[int], ge=0, le=10, "Number of automatic retries for failed backend connections."
    - retry_delay: Optional[float], ge=0, le=120, "Seconds to wait between retry attempts."

    Do not change any other part of the file.
  </action>
  <verify>
    <automated>grep -c "startup\|retries\|retry_delay" /home/diaz/mygit/argus-mcp-docs/contents/docs/configuration/backends/index.mdx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "startup" contents/docs/configuration/backends/index.mdx` — returns at least 2 matches (YAML example + table row)
    - `grep "retries" contents/docs/configuration/backends/index.mdx` — returns at least 2 matches (YAML example + table row)
    - `grep "retry_delay" contents/docs/configuration/backends/index.mdx` — returns at least 2 matches (YAML example + table row)
    - The table row for `startup` includes description text about "subprocess spawn" (matching schema_backends.py description)
    - The table row for `retries` includes constraint "(0–10)" matching the le=10 in schema_backends.py
    - The table row for `retry_delay` includes constraint "(0–120)" matching the le=120 in schema_backends.py
  </acceptance_criteria>
  <done>The Timeouts section now documents all 6 fields from TimeoutConfig: init, cap_fetch, sse_startup, startup, retries, retry_delay.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "eight\|8" contents/docs/plugins/index.mdx` — confirms accurate plugin count language
2. `grep -c "^## " contents/docs/plugins/built-in-plugins/index.mdx` — confirms 9 sections (8 plugins + Combined Example)
3. `grep "startup\|retries\|retry_delay" contents/docs/configuration/backends/index.mdx | wc -l` — returns 6+ (2+ per field)
</verification>

<success_criteria>
- plugins/index.mdx: accurate plugin count claim
- plugins/built-in-plugins/index.mdx: all 8 plugins documented with correct settings derived from source
- configuration/backends/index.mdx: all 6 timeout fields (init, cap_fetch, sse_startup, startup, retries, retry_delay) in both example and table
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-accuracy/04-01-SUMMARY.md`
</output>
