---
phase: 01-catalog-automation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml
  - /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml
autonomous: true
requirements:
  - CATALOG-02
  - CATALOG-03
  - CATALOG-04
  - CATALOG-05

must_haves:
  truths:
    - "generate-index.yml exists with path filter on configs/**/*.yaml, GITHUB_TOKEN commit-back via git-auto-commit-action@v7 with [skip ci], and concurrency group catalog-index-generation with cancel-in-progress: false"
    - "notify-docs.yml exists and triggers via workflow_run on 'Generate Catalog Index' success, dispatches deploy.yml on diaz3618/argus-mcp-docs using DOCS_DISPATCH_TOKEN"
    - "DOCS_DISPATCH_TOKEN setup instructions are co-located as a comment block at the top of notify-docs.yml"
  artifacts:
    - path: "/home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml"
      provides: "Auto-commit CI workflow — runs on configs/** push, commits catalog.json back to main"
      contains: "stefanzweifel/git-auto-commit-action@v7"
    - path: "/home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml"
      provides: "Cross-repo dispatch workflow — chains off generate-index success and triggers docs rebuild"
      contains: "gh workflow run deploy.yml"
  key_links:
    - from: "generate-index.yml"
      to: "notify-docs.yml"
      via: "workflow_run trigger: workflows: [\"Generate Catalog Index\"]"
      pattern: "workflow_run"
    - from: "notify-docs.yml"
      to: "argus-mcp-docs deploy.yml"
      via: "gh workflow run deploy.yml --repo diaz3618/argus-mcp-docs"
      pattern: "gh workflow run deploy\\.yml"
    - from: "generate-index.yml"
      to: "catalog.json"
      via: "stefanzweifel/git-auto-commit-action@v7 with GITHUB_TOKEN"
      pattern: "git-auto-commit-action"
---

<objective>
Write two GitHub Actions workflow files in `argus-mcp-catalog`:
1. `.github/workflows/generate-index.yml` — fires on push to main when `configs/**` changes, runs `generate-index.js`, commits `catalog.json` back via GITHUB_TOKEN (loop-safe) with `[skip ci]`, serialized by a concurrency group
2. `.github/workflows/notify-docs.yml` — chains off `generate-index.yml` success via `workflow_run`, dispatches `deploy.yml` on `diaz3618/argus-mcp-docs` using `DOCS_DISPATCH_TOKEN`; includes PAT setup instructions as a comment block at the top (per D-01)

Purpose: These two workflows form the automation loop — every merge to `argus-mcp-catalog` that touches YAML configs regenerates `catalog.json` and triggers an `argus-mcp-docs` rebuild automatically.
Output: Two new workflow files committed to `argus-mcp-catalog`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-catalog-automation/01-RESEARCH.md
@.planning/phases/01-catalog-automation/01-CONTEXT.md
@.planning/phases/01-catalog-automation/01-01-SUMMARY.md

<interfaces>
<!-- Key patterns extracted from codebase. Use these directly. -->

From /home/diaz/mygit/argus-mcp-catalog/.github/workflows/lint-catalog.yml (job structure to match):
```yaml
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
```

From /home/diaz/mygit/argus-mcp-docs/.github/workflows/deploy.yml (dispatch target — confirmed has workflow_dispatch):
- Has `on: workflow_dispatch:` — this is the target for notify-docs.yml
- Has `concurrency: group: pages, cancel-in-progress: false` — docs deploys already serialized

DOCS_DISPATCH_TOKEN PAT setup (exact values from RESEARCH.md):
```
GitHub.com → Settings → Developer settings → Personal access tokens → Fine-grained tokens
  Token name: argus-mcp-docs-dispatch
  Resource owner: diaz3618
  Repository access: Only select repositories → argus-mcp-docs
  Permissions:
    Repository permissions → Actions: Read and Write
    (Metadata: Read — auto-selected)
    All other: No access

Store as:
  argus-mcp-catalog → Settings → Secrets and variables → Actions → DOCS_DISPATCH_TOKEN
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .github/workflows/generate-index.yml</name>
  <files>/home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml</files>
  <read_first>
    - /home/diaz/mygit/argus-mcp-catalog/.github/workflows/lint-catalog.yml (job structure and action version conventions)
    - /home/diaz/mygit/argus-mcp-docs/.planning/phases/01-catalog-automation/01-RESEARCH.md (Pattern 1 — Auto-Commit Generated File, contains exact verbatim YAML)
  </read_first>
  <action>
Create `/home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml` with the following exact content (verbatim from RESEARCH.md Pattern 1):

```yaml
name: Generate Catalog Index

on:
  push:
    branches:
      - main
    paths:
      - "configs/**/*.yaml"

concurrency:
  group: catalog-index-generation
  cancel-in-progress: false

jobs:
  generate-index:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Generate catalog.json
        run: node scripts/generate-index.js

      - uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: "chore: regenerate catalog.json [skip ci]"
          file_pattern: "catalog.json"
          commit_user_name: "github-actions[bot]"
          commit_user_email: "41898282+github-actions[bot]@users.noreply.github.com"
          commit_author: "github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>"
```

Critical requirements — verify all are present:
- `name: Generate Catalog Index` (exact — must match the `workflows:` list in notify-docs.yml character-for-character, per Pitfall 5)
- `paths: "configs/**/*.yaml"` (not `configs/**` — scoped to YAML files only)
- `concurrency: group: catalog-index-generation` with `cancel-in-progress: false` (not `true` — per Pitfall 2 and CATALOG-03)
- `permissions: contents: write` at **job level** (not workflow level — per anti-pattern in RESEARCH.md)
- `stefanzweifel/git-auto-commit-action@v7` (not v5 or v6)
- `commit_message` contains `[skip ci]` (belt-and-suspenders against infinite loop, per Pitfall 1)
- `GITHUB_TOKEN` is used implicitly by git-auto-commit-action (do NOT add an explicit `token:` using a PAT — per anti-pattern in RESEARCH.md)
  </action>
  <verify>
    <automated>
grep "Generate Catalog Index" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml && grep "catalog-index-generation" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml && grep "cancel-in-progress: false" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml && grep "stefanzweifel/git-auto-commit-action@v7" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml && grep "\[skip ci\]" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml && grep "configs/\*\*" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml && grep "contents: write" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "name: Generate Catalog Index" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml` returns a match
    - `grep "catalog-index-generation" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml` returns a match
    - `grep "cancel-in-progress: false" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml` returns a match (not `true`)
    - `grep "stefanzweifel/git-auto-commit-action@v7" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml` returns a match
    - `grep "\[skip ci\]" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml` returns a match
    - `grep "configs/\*\*" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml` returns a match
    - `grep "contents: write" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml` returns a match
    - `grep "permissions:" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml` returns exactly one match and it appears inside the `generate-index:` job block (not at workflow level)
  </acceptance_criteria>
  <done>
    `generate-index.yml` exists with correct path filter, concurrency group (cancel-in-progress: false), job-level permissions, git-auto-commit-action@v7, and [skip ci] in commit message.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .github/workflows/notify-docs.yml and commit both workflows</name>
  <files>/home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml</files>
  <read_first>
    - /home/diaz/mygit/argus-mcp-catalog/.github/workflows/generate-index.yml (just written — confirm exact `name:` value to copy into workflow_run trigger)
    - /home/diaz/mygit/argus-mcp-docs/.planning/phases/01-catalog-automation/01-RESEARCH.md (Pattern 2 — Cross-Repo Workflow Dispatch via workflow_run, contains exact verbatim YAML)
    - /home/diaz/mygit/argus-mcp-docs/.planning/phases/01-catalog-automation/01-CONTEXT.md (D-01 — comment block location and required content)
  </read_first>
  <action>
Create `/home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml`.

The file has two parts: a YAML block comment at the top (per D-01 from CONTEXT.md) followed by the workflow YAML.

**Part 1 — DOCS_DISPATCH_TOKEN setup comment block (per D-01):**
Place this comment block at the very top of the file, before the `name:` key:

```yaml
# DOCS_DISPATCH_TOKEN Setup
#
# This workflow requires a fine-grained Personal Access Token stored as a
# repository secret in argus-mcp-catalog named DOCS_DISPATCH_TOKEN.
#
# To create the token:
#   1. Go to: GitHub.com → Settings → Developer settings →
#              Personal access tokens → Fine-grained tokens → Generate new token
#   2. Token name: argus-mcp-docs-dispatch
#   3. Resource owner: diaz3618
#   4. Repository access: Only select repositories → argus-mcp-docs
#   5. Permissions:
#        Repository permissions → Actions: Read and Write
#        (Metadata: Read is auto-selected — leave it)
#        All other permissions: No access
#   6. Generate token and copy the value
#
# To store the secret:
#   argus-mcp-catalog → Settings → Secrets and variables → Actions →
#   New repository secret → Name: DOCS_DISPATCH_TOKEN → paste token value
```

**Part 2 — Workflow YAML (verbatim from RESEARCH.md Pattern 2):**

```yaml
name: Notify Docs Rebuild

on:
  workflow_run:
    workflows: ["Generate Catalog Index"]
    types: [completed]
    branches: [main]

jobs:
  dispatch:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Trigger argus-mcp-docs rebuild
        env:
          GH_TOKEN: ${{ secrets.DOCS_DISPATCH_TOKEN }}
        run: |
          if ! gh workflow run deploy.yml \
               --repo diaz3618/argus-mcp-docs \
               --ref main; then
            echo "ERROR: Failed to dispatch deploy.yml on argus-mcp-docs" >&2
            echo "Check that DOCS_DISPATCH_TOKEN has actions:write on diaz3618/argus-mcp-docs" >&2
            exit 1
          fi
          echo "Dispatch succeeded — deploy.yml queued on argus-mcp-docs"
```

Critical requirements — verify all are present:
- `workflows: ["Generate Catalog Index"]` — must match the `name:` field in `generate-index.yml` EXACTLY (character-for-character, per Pitfall 5)
- `types: [completed]` with `if: github.event.workflow_run.conclusion == 'success'` guard (only dispatch on success, not on failure)
- `GH_TOKEN: ${{ secrets.DOCS_DISPATCH_TOKEN }}` (not GITHUB_TOKEN — GITHUB_TOKEN cannot authenticate to a different repo)
- `--repo diaz3618/argus-mcp-docs` (scoped to docs repo, not catalog repo)
- No `concurrency:` block on this workflow — deploy.yml already has `concurrency: group: pages, cancel-in-progress: false`

After writing both files, commit them together (per D-03 — one atomic commit for the workflows plan):
```
cd /home/diaz/mygit/argus-mcp-catalog
git add .github/workflows/generate-index.yml .github/workflows/notify-docs.yml
git commit -m "feat(workflows): add generate-index and notify-docs workflows for catalog automation"
```
  </action>
  <verify>
    <automated>
grep "workflow_run" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml && grep '"Generate Catalog Index"' /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml && grep "conclusion == 'success'" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml && grep "DOCS_DISPATCH_TOKEN" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml && grep "gh workflow run deploy.yml" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml && grep "diaz3618/argus-mcp-docs" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml && grep "DOCS_DISPATCH_TOKEN Setup" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "workflow_run" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml` returns a match
    - `grep '"Generate Catalog Index"' /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml` returns a match (double-quoted, exact string)
    - `grep "conclusion == 'success'" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml` returns a match
    - `grep "DOCS_DISPATCH_TOKEN" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml` returns at least 2 matches (once in GH_TOKEN env var, once in comment block)
    - `grep "gh workflow run deploy.yml" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml` returns a match
    - `grep "diaz3618/argus-mcp-docs" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml` returns a match
    - `grep "DOCS_DISPATCH_TOKEN Setup" /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml` returns a match (comment block present)
    - `head -1 /home/diaz/mygit/argus-mcp-catalog/.github/workflows/notify-docs.yml` returns `# DOCS_DISPATCH_TOKEN Setup` (comment is first line)
    - `git -C /home/diaz/mygit/argus-mcp-catalog log --oneline -1` shows a commit message containing both `generate-index` and `notify-docs`
  </acceptance_criteria>
  <done>
    `notify-docs.yml` exists with workflow_run trigger on "Generate Catalog Index", success guard, DOCS_DISPATCH_TOKEN dispatch, and PAT setup instructions as a comment block at the top. Both workflow files committed in a single atomic commit.
  </done>
</task>

</tasks>

<verification>
From `/home/diaz/mygit/argus-mcp-catalog/`:

```bash
# CATALOG-02: path filter, auto-commit, [skip ci]
grep -E "configs/\*\*|git-auto-commit-action@v7|\[skip ci\]" .github/workflows/generate-index.yml
# Expected: 3 matches

# CATALOG-03: concurrency group
grep -E "catalog-index-generation|cancel-in-progress: false" .github/workflows/generate-index.yml
# Expected: 2 matches

# CATALOG-04: workflow_run chain and dispatch
grep -E "workflow_run|Generate Catalog Index|conclusion.*success|gh workflow run deploy" .github/workflows/notify-docs.yml
# Expected: 4 matches

# CATALOG-05: token documented in comment
grep "DOCS_DISPATCH_TOKEN" .github/workflows/notify-docs.yml
# Expected: multiple matches (comment + env var)
```
</verification>

<success_criteria>
- `generate-index.yml` exists with correct trigger, concurrency group (cancel-in-progress: false), job-level permissions, git-auto-commit-action@v7, and [skip ci] in commit message
- `notify-docs.yml` exists with workflow_run trigger on exact workflow name "Generate Catalog Index", success guard, DOCS_DISPATCH_TOKEN dispatch to diaz3618/argus-mcp-docs, and PAT setup comment block at top of file
- Both files committed to `argus-mcp-catalog` in a single atomic commit
</success_criteria>

<output>
After completion, create `.planning/phases/01-catalog-automation/01-02-SUMMARY.md`
</output>
