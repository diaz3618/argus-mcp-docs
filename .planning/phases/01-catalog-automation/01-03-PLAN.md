---
phase: 01-catalog-automation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - /home/diaz/mygit/argus-mcp-catalog/.github/pull_request_template.md
autonomous: false
requirements:
  - CONTRIB-01

must_haves:
  truths:
    - "Contributors opening a PR to argus-mcp-catalog see a checklist template with required YAML fields and category placement guidance"
    - "Template covers name:, description:, backend-slug key, and configs/{category}/ directory placement — the same fields enforced by lint-catalog.js"
  artifacts:
    - path: "/home/diaz/mygit/argus-mcp-catalog/.github/pull_request_template.md"
      provides: "PR template guiding contributors on required YAML fields"
      contains: "pull_request_template.md"
  key_links:
    - from: ".github/pull_request_template.md"
      to: "scripts/lint-catalog.js"
      via: "Checklist fields match the three lint requirements: name, description, backend-slug key"
      pattern: "name:|description:"
---

<objective>
Write `.github/pull_request_template.md` in `argus-mcp-catalog` — a PR checklist that guides contributors on the three required YAML fields (`name:`, `description:`, backend-slug key) and correct category directory placement. Commit and push all Phase 1 changes to `argus-mcp-catalog`.

Purpose: Reduces contributor errors before they reach CI lint. The template surfaces the same requirements enforced by `lint-catalog.js`.
Output: `.github/pull_request_template.md` committed and all Phase 1 changes pushed to `argus-mcp-catalog` remote.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-catalog-automation/01-CONTEXT.md
@.planning/phases/01-catalog-automation/01-RESEARCH.md
@.planning/phases/01-catalog-automation/01-01-SUMMARY.md
@.planning/phases/01-catalog-automation/01-02-SUMMARY.md

<interfaces>
<!-- Required fields from lint-catalog.js validation logic -->

From /home/diaz/mygit/argus-mcp-catalog/scripts/lint-catalog.js (the lint rules contributors must satisfy):
```javascript
// 1. name: — non-empty string
if (typeof parsed.name !== 'string' || parsed.name.trim() === '') {
  addError(`... missing or empty top-level "name:" string`);
}
// 2. description: — non-empty string
if (typeof parsed.description !== 'string' || parsed.description.trim() === '') {
  addError(`... missing or empty top-level "description:" string`);
}
// 3. At least one backend-slug key (any key besides name and description)
const reserved = new Set(['name', 'description']);
const backendKeys = Object.keys(parsed).filter(k => !reserved.has(k));
if (backendKeys.length === 0) {
  addError(`... no backend-slug key found`);
}
```

configs/ directory structure (category placement):
```
configs/{category}/{filename}.yaml
# e.g.: configs/filesystem-access/my-tool.yaml
```

Existing categories (from catalog.json):
filesystem-access, web-research, databases, ai-memory, devops-integrations,
security-tools, remote-sse, remote-http, remote-auth, fully-isolated
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .github/pull_request_template.md and commit</name>
  <files>/home/diaz/mygit/argus-mcp-catalog/.github/pull_request_template.md</files>
  <read_first>
    - /home/diaz/mygit/argus-mcp-catalog/scripts/lint-catalog.js (the three lint rules the checklist must cover: name, description, backend-slug key)
    - /home/diaz/mygit/argus-mcp-docs/.planning/phases/01-catalog-automation/01-CONTEXT.md (D-02 — format: checklist items with one-line inline examples, no full YAML block)
  </read_first>
  <action>
Create `/home/diaz/mygit/argus-mcp-catalog/.github/pull_request_template.md`.

Format requirements (per D-02 from CONTEXT.md):
- Checklist items with one-line inline examples
- Each required field gets a checkbox and a one-line example
- No full YAML block — concise and actionable
- Covers exactly: name:, description:, backend-slug key presence, category directory placement

The file should look like this (use Claude's discretion for exact wording and tone):

```markdown
## YAML Config Checklist

Before merging, verify your YAML file has the required fields:

- [ ] `name:` is present and non-empty — e.g., `name: My Tool`
- [ ] `description:` is present and non-empty — e.g., `description: Does something useful`
- [ ] At least one backend-slug key is present (any key besides `name` and `description`) — e.g., `stdio: ...` or `docker: ...`
- [ ] File is placed in the correct category directory — e.g., `configs/filesystem-access/my-tool.yaml`

> Run `node scripts/lint-catalog.js` locally to validate before pushing.
```

Wording is at Claude's discretion — the exact phrasing above is a guide, not a requirement. What IS required:
1. A checkbox (`- [ ]`) for `name:`
2. A checkbox for `description:`
3. A checkbox for backend-slug key (any key besides name and description)
4. A checkbox for `configs/{category}/` directory placement
5. Each checkbox has a one-line inline example (e.g., `name: My Tool`)
6. No multi-line YAML code block

After writing the file, commit it (per D-03 — one atomic commit for this plan):
```
cd /home/diaz/mygit/argus-mcp-catalog
git add .github/pull_request_template.md
git commit -m "docs: add pull_request_template with required YAML field checklist"
```
  </action>
  <verify>
    <automated>
grep -E "name:|description:" /home/diaz/mygit/argus-mcp-catalog/.github/pull_request_template.md && grep "\- \[ \]" /home/diaz/mygit/argus-mcp-catalog/.github/pull_request_template.md && grep "configs/" /home/diaz/mygit/argus-mcp-catalog/.github/pull_request_template.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "\- \[ \]" /home/diaz/mygit/argus-mcp-catalog/.github/pull_request_template.md` returns at least 4 matches (4 checklist items)
    - `grep "name:" /home/diaz/mygit/argus-mcp-catalog/.github/pull_request_template.md` returns a match
    - `grep "description:" /home/diaz/mygit/argus-mcp-catalog/.github/pull_request_template.md` returns a match
    - `grep "configs/" /home/diaz/mygit/argus-mcp-catalog/.github/pull_request_template.md` returns a match (category directory placement documented)
    - `git -C /home/diaz/mygit/argus-mcp-catalog log --oneline -1` shows a commit message containing `pull_request_template`
  </acceptance_criteria>
  <done>
    `.github/pull_request_template.md` exists with checklist items for name:, description:, backend-slug key, and configs/{category}/ placement — each with a one-line inline example. Committed to the catalog repo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Push all Phase 1 commits to remote</name>
  <files>N/A — git push operation only</files>
  <read_first>
    - Run `git -C /home/diaz/mygit/argus-mcp-catalog log --oneline -5` to confirm the three Phase 1 commits are present before pushing
  </read_first>
  <action>
Verify the three Phase 1 commits are present in the local git history, then push to remote:

```bash
cd /home/diaz/mygit/argus-mcp-catalog

# Confirm the three commits exist
git log --oneline -5

# Push to main
git push origin main
```

The three commits that must appear in the log (in order, most recent first):
1. `docs: add pull_request_template with required YAML field checklist`
2. `feat(workflows): add generate-index and notify-docs workflows for catalog automation`
3. `feat(scripts): add generate-index.js to auto-generate catalog.json`

If any commit is missing, stop and investigate before pushing. Do not push a partial set.

After a successful push, print the remote URL and the GitHub Actions URL for the user to monitor the first workflow run:
```
echo "Pushed to: https://github.com/diaz3618/argus-mcp-catalog"
echo "Monitor workflows: https://github.com/diaz3618/argus-mcp-catalog/actions"
```
  </action>
  <verify>
    <automated>
git -C /home/diaz/mygit/argus-mcp-catalog log --oneline -4
    </automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/diaz/mygit/argus-mcp-catalog log --oneline -4` shows all three Phase 1 commits in history
    - `git -C /home/diaz/mygit/argus-mcp-catalog status` shows `Your branch is up to date with 'origin/main'` (or equivalent clean state after push)
    - `git -C /home/diaz/mygit/argus-mcp-catalog log --oneline origin/main -3` matches local log (remote has the commits)
  </acceptance_criteria>
  <done>
    All three Phase 1 commits pushed to `origin/main` on `argus-mcp-catalog`. Remote is up to date with local.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Create DOCS_DISPATCH_TOKEN and verify Phase 1 on GitHub</name>
  <files>N/A — human-only action, no files modified by Claude</files>
  <action>
Human action required (CATALOG-05 — cannot be automated). The user must:
1. Create a fine-grained PAT named `argus-mcp-docs-dispatch` scoped to the `argus-mcp-docs` repository with Actions: Read and Write permission.
2. Store it as a secret named `DOCS_DISPATCH_TOKEN` in `argus-mcp-catalog` repository settings.
See `<how-to-verify>` below for exact step-by-step instructions.
  </action>
  <verify>
    <automated>MISSING — this is a checkpoint:human-verify task; verification is performed manually via browser per steps below</automated>
  </verify>
  <what-built>
All Phase 1 artifacts have been committed and pushed to argus-mcp-catalog:
- scripts/generate-index.js (CATALOG-01)
- .github/workflows/generate-index.yml (CATALOG-02, CATALOG-03)
- .github/workflows/notify-docs.yml (CATALOG-04)
- .github/pull_request_template.md (CONTRIB-01)

CATALOG-05 (DOCS_DISPATCH_TOKEN) requires manual action — instructions are in the comment block at the top of notify-docs.yml and are reproduced below.
  </what-built>
  <how-to-verify>
**Step 1 — Create DOCS_DISPATCH_TOKEN (CATALOG-05):**

1. Go to: https://github.com/settings/personal-access-tokens/new
2. Token name: `argus-mcp-docs-dispatch`
3. Resource owner: `diaz3618`
4. Repository access: "Only select repositories" — select `argus-mcp-docs` only
5. Permissions — Repository permissions — Actions: **Read and Write** (Metadata: Read is auto-selected — leave it)
6. Generate token and copy the value

7. Go to: https://github.com/diaz3618/argus-mcp-catalog/settings/secrets/actions
8. Click "New repository secret"
9. Name: `DOCS_DISPATCH_TOKEN`
10. Value: paste the token you copied
11. Click "Add secret"

**Step 2 — Verify workflows appeared in GitHub Actions:**

1. Visit: https://github.com/diaz3618/argus-mcp-catalog/actions
2. Confirm "Generate Catalog Index" and "Notify Docs Rebuild" workflows are listed
3. The push of generate-index.yml may have already triggered "Generate Catalog Index" once — check if it ran and committed a new catalog.json with a live timestamp

**Step 3 — Inspect files (optional spot-check):**

- https://github.com/diaz3618/argus-mcp-catalog/blob/main/scripts/generate-index.js
- https://github.com/diaz3618/argus-mcp-catalog/blob/main/.github/workflows/generate-index.yml
- https://github.com/diaz3618/argus-mcp-catalog/blob/main/.github/workflows/notify-docs.yml
  </how-to-verify>
  <resume-signal>
Type "approved" when DOCS_DISPATCH_TOKEN is created and stored, or describe any issues found.
  </resume-signal>
  <done>
    DOCS_DISPATCH_TOKEN created as a fine-grained PAT (Actions: Read and Write on argus-mcp-docs) and stored as a secret in argus-mcp-catalog. Both "Generate Catalog Index" and "Notify Docs Rebuild" workflows visible in GitHub Actions tab.
  </done>
</task>

</tasks>

<verification>
From `/home/diaz/mygit/argus-mcp-catalog/`:

```bash
# All Phase 1 files present
ls scripts/generate-index.js .github/workflows/generate-index.yml .github/workflows/notify-docs.yml .github/pull_request_template.md
# Expected: all 4 files listed

# Commits on remote
git log --oneline origin/main -5
# Expected: 3 Phase 1 commits visible

# PR template has all required fields
grep -c "\- \[ \]" .github/pull_request_template.md
# Expected: 4 (four checklist items)
```
</verification>

<success_criteria>
- `.github/pull_request_template.md` exists with 4 checklist items covering name:, description:, backend-slug key, and configs/{category}/ placement
- All three Phase 1 commits pushed to `origin/main`
- User has created `DOCS_DISPATCH_TOKEN` fine-grained PAT (Actions: Read and Write on argus-mcp-docs) and stored it as a secret in argus-mcp-catalog
- GitHub Actions tab at github.com/diaz3618/argus-mcp-catalog/actions shows both new workflows listed
</success_criteria>

<output>
After completion, create `.planning/phases/01-catalog-automation/01-03-SUMMARY.md`
</output>
